<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened Runtime entitlements for the signed/notarized MacPi build.

  Hardened Runtime is mandatory for notarization (macOS 10.15+). These keys
  relax the parts of it that a stock Electron app — plus pi-coding-agent's
  unpacked native modules — need in order to run:

  - allow-jit / allow-unsigned-executable-memory: Electron's V8 engine JITs
    JavaScript into executable memory at runtime.
  - disable-library-validation: @earendil-works/* and the wasm-backed
    @silvia-odwyer/photon-node are unpacked out of the asar (see
    forge.config.ts) and loaded at runtime. They are not signed with this
    app's Developer ID, so library validation must be relaxed to load them.
  - allow-dyld-environment-variables: pi-coding-agent spawns helper
    subprocesses that rely on DYLD_* environment variables.
-->
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
  </dict>
</plist>
